<resources>
    <!-- Base application theme. -->
    <style name="Theme.MarvelComics" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/dark_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="customCutImageView" parent="">
        <item name="cornerFamily">cut</item>
        <item name="cornerSize">0dp</item>
        <item name="cornerSizeBottomRight">24dp</item>
    </style>

    <style name="Theme.MarvelComics.splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.MarvelComics</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>

    </style>

    <style name="progressBarStyle" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/red</item>
    </style>
</resources>